{
    "network": {
        "servers": [ 
            {% for node in monitoring_nodes %}
                "{{ node }}" {% if not loop.last %},{% endif %}
            {% endfor %} 
        ],
        "ssl certificate": "{{ cert_dir }}/{{ app_name }}.crt",
        "ssl key": "{{ cert_dir }}/{{ app_name }}.key",
        "ssl ca": "{{ cert_dir }}/{{ app_name }}.crt"
    },
    "files": [
        {
            "paths":  [
                {% for dir in nginx_log_dirs %}
                    "{{ dir }}" {% if not loop.last %},{% endif %}
                {% endfor %} 
            ],
            "fields": { "type": "nginx-access" }
        }
    ]
}